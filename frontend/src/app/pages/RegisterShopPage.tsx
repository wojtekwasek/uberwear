export function RegisterShopPage() {
  return (
    <div className="min-h-[50vh] bg-[var(--soft-surface)] text-[var(--navy)]">
      <div className="page-container py-12">
        <div className="card p-8 text-center">
          <h1 className="text-3xl font-semibold">Register a shop</h1>
          <p className="mt-3 text-sm text-[var(--muted)]">
            Shop onboarding lives on the backend. This screen stays simple for now—drop us a message on the Contact page
            and we will plug your catalog into Uberwear.
          </p>
        </div>
      </div>
    </div>
  );
}
